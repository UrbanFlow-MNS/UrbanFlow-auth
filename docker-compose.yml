services:
  user-database:
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - user_data:/var/lib/postgresql/data

  auth-service:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env

volumes:
  user_data: