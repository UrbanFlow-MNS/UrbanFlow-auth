services:
  user-database:
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - user_data:/var/lib/postgresql/data

  auth-service:
    build: .
    ports:
      - "4001:4001"
    env_file:
      - .env

volumes:
  user_data: